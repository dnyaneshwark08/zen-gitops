apiVersion: v1
kind: Service
metadata:
  name: qc-service
  namespace: dev
spec:
  type: ClusterIP
  selector:
    app: qc-service
  ports:
    - port: 8086
      targetPort: 8086
      protocol: TCP
